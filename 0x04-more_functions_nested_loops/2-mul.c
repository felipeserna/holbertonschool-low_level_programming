#include "holberton.h"
/**
 * mul - function that multiplies two integers
 * @a: Letter to check
 * @b: Letter to check
 * Return: result
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}

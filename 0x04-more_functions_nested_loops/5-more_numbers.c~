#include "holberton.h"
/**
 * more_numbers - prints 10 times 0 to 14, followed by a new line
 * c: Letter to check
 * Return: nothing
 */
void more_numbers(void)
{
	int i;
	int j;

	for (j = 0; j <= 9; j++)
	{
		for (i = 0; i <= 9; i++)
		{
			_putchar(i + '0');
		}
		_putchar(j + '0');
		_putchar('\n');
	}
}

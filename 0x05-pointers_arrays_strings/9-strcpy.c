#include "holberton.h"
/**
 * _strcpy - copies the string
 * @dest: string to check
 * @src: string to check
 * Return: 0
 */
char *_strcpy(char *dest, char *src)
{
	*dest = 'a';
	*src = 'b';
	return (0);
}

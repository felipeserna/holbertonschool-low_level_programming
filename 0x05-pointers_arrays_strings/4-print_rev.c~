#include "holberton.h"
/**
 * print_rev - prints a string, in reverse, followed by a new line
 * @str: Letter to check
 * Return: nothing
 */
void print_rev(char *s)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
	_putchar(str[i]);
	}
	_putchar('\n');
}

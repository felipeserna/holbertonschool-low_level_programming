#include "holberton.h"
/**
 * puts_half - prints half of a string, followed by a new line
 * @str: Letter to check
 * Return: nothing
 */
void puts_half(char *str)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
		if (i % 2 == 0)
		{
			_putchar(str[i]);
		}
	}
	_putchar('\n');
}

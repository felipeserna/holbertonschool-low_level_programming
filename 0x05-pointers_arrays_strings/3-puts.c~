#include "holberton.h"
/**
 * _puts - prints a string, followed by a new line, to stdout
 * @s: Letter to check
 * Return: length of a string
 */
void _puts(char *str)
{
	_putchar(str);
	_putchar('\n');
}

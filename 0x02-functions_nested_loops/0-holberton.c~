#include "holberton.h"
/**
 * alphabet - array for printing Holberton
 * c: letter to check
 * Return: void
 */

int main(void)
{
	char p[10] = {'H', 'o', 'l', 'b', 'e', 'r', 't', 'o', 'n', '\n'};
	int c;

	for (c = 0; c < 10; c++)
		_putchar(p[c]);
	return (0);
}
